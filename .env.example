# Database Configuration
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/conversation_vault

# Server Configuration
HOST=127.0.0.1
PORT=7025

# Security
SECRET_KEY=your-secret-key-here-generate-a-random-one

# Encryption Key for API Keys (base64 encoded, generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your-fernet-key-here

# Environment
ENV=development

# Optional: Provider API Keys can be stored here or managed via UI
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
